/**
 * 快速排序
 * 1. 确定分界点
 * 2. 左右两边排序
 * 3. 递归左右两边
 */
const rl = require("readline")
const readLine = rl.createInterface({
    input: process.stdin,
    output:process.stdin
});

let anw = "";

readLine.on("line",arr => {
    // 读取数据，调用函数    
})

readLine.on("close",() => {
console.log()  
}) 

